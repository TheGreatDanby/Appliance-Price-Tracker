{
  "name": "dishwasher-price-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily price tracker for the Bosch Series 6 60cm Built-Under Dishwasher across AU retailers. Free GitHub Actions + GitHub Pages, no paid services.",
  "scripts": {
    "check": "node scripts/check-prices.mjs",
    "build": "node scripts/generate-dashboard.mjs",
    "check-and-build": "npm run check && npm run build"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
